# Netlify：发布目录为仓库根下 dist（与 postbuild 生成的 _redirects 一致）
# 构建须包含 postbuild（根 index / 404 / _redirects），否则主应用壳层与刷新回退不完整
[build]
  command = "pnpm i && pnpm run build && pnpm run postbuild"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
