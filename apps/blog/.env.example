# 本地开发时，blog 默认请求 http://localhost:8000 的 api-server。
# 若本地 api-server 未配置 DATABASE_URL，列表只会是内置的一条模拟数据。
#
# 需要直接看「线上」接口返回的数据时，可指定线上 Deno 部署地址（与生产同源）：
# VITE_API_SERVER_BASE=https://blog-server.promisezhangbo.deno.net
