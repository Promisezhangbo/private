{
  "name": "blog-server",
  "description": "博客后端：Fetch 风格 handler，Node 本地与 Deno Deploy 共用；路由见 src/handler.js。",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "postgres": "3.4.5"
  },
  "scripts": {
    "dev": "node --watch src/node.js",
    "start": "node src/node.js",
    "build": "node scripts/check-syntax.mjs",
    "lint": "oxlint --fix src",
    "deploy:dev": "deno task dev"
  }
}
